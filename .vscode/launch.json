{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start Expo",
            "type": "pwa-node",
            "request": "launch",
            "runtimeExecutable": "npx",
            "runtimeArgs": ["expo", "start"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}